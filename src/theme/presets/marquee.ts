import type { ThemePreset } from "../tokens";

/** Warm paper, marquee red — a printed playbill in daylight. */
export const marquee: ThemePreset = {
  name: "marquee",
  colors: {
    base: "#EDE8DF",
    text: "#1A1714",
    muted: "#6B6459",
    accent: "#8A2B1F",
    onAccent: "#EDE8DF",
  },
};
